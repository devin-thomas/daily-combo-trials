[project]
name = "daily-combo-trials"
version = "0.1.0"
description = "A daily fighting-game combo trial challenge site built with FastAPI."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.vercel]
entrypoint = "app:app"
